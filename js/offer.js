import { offers } from "./data";

function createOffer(offer){
1 пойти и найти елемент с айди кард
2 клонируем елемент со всеми внутреними елементами
3 для склоненого елемента меняем значения на те которые в оффере
}

offers.forEach((el)=>{
    createOffer(el)
})